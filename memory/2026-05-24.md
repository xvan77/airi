# Daily Memory Log - 2026-05-24

## Durable Facts & Decisions
- **Strict No-Push Policy Committed**: Added explicit remote git push restrictions to `AGENTS.md`. No automated git pushes are allowed unless the user explicitly requests it in chat.
- **Coordinate Resolution Fix**: Discovered that `pixi-live2d-display` `hitTest()` expects global coordinates. Standardized coordinate transformation from client `clientX/clientY` to logical coordinates:
  ```typescript
  const globalX = mouseX * (props.app.screen.width / rect.width)
  const globalY = mouseY * (props.app.screen.height / rect.height)
  ```
- **Smart hitArea Matching**: Implemented a cascade fallback matching rule to bridge the gap between `hitTest` hitArea names (e.g. `DRETouchBoxHandR`) and their corresponding authored motion group definitions (e.g. `DREFTouchBoxHand`):
  1. Exact case-insensitive match.
  2. Group starts with hitArea.
  3. hitArea starts with group.
  4. Group contains hitArea.
  5. hitArea contains group.
- **Commit & Push Success**: Successfully committed changes locally and pushed upstream upon explicit user request and approval.

## User Preferences
- **Push Policy**: Never push to remote branch without explicit interactive authorization.
- **Tactile Feedback**: Prefers direct click-to-motion-audio triggers and pink glow overlay centering on custom coordinates.
